# torch は setup.sh で CUDA 指定インストールするため除外
setuptools
wheel
transformers>=4.51.0
accelerate>=1.0.0
deepspeed>=0.15.0
datasets>=3.0.0
sentencepiece
protobuf
packaging
ninja
# FlashAttention2 は setup.sh で --no-build-isolation 付きで個別インストール
# flash-attn>=2.5.0
